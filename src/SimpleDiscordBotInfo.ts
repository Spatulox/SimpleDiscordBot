import pkg from '../package.json'

export const SimpleDiscordBotInfo = {
    name: pkg.name,
    version: pkg.version,
    author: pkg.author,
    description: pkg.description,
    license: pkg.license
}